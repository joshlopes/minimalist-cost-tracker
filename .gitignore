# SDD Control — host writes feature artifacts here; never check them in.
.luis-mission-control/
.mission-control/
.worktrees/

# Common noise — extend per language once the stack is picked.
.DS_Store
*.log
node_modules/
dist/
build/
.env
.env.local

# Go
/bin/
*.test
*.out

# Local SQLite databases
*.db
*.db-wal
*.db-shm
